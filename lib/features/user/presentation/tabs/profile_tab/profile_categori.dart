import 'package:flutter/material.dart';

class ProfileCategori extends StatelessWidget {
  const ProfileCategori({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("p");
  }
}
